Axis is a modern, GL-based interface coded in python

The principal Axis gui sim ini files:

axis_metric.ini     basic demo (mm)
axis_imperial.ini   basic demo (in)

These configurations demonstrate LinuxCNC functionality using the axis gui as the interface.